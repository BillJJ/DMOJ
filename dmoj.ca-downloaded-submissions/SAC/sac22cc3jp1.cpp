#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

const int MM = 1e5+5;


int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);

    string s; cin >> s;
    cout << (s.find("demello")!=-1? "liar":"what are we going to do?") << "\n";

    return 0;
}